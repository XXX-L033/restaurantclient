import React from 'react';

const NotFound = (props) => (
  <div>
    <h1> If you can see this message, you are ready to develop the imp res! </h1>
    <div> Will be Replace by 404 page </div>
  </div>
);

export default NotFound;
